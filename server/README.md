 # Operation Vault Server
This is the server for Operation Vault. It basically acts as a relay between clients while also acting as the gate to the cloud storage. It's written in Go and upgrades connection from clients to WebTransport. There really isn't much else to say...
